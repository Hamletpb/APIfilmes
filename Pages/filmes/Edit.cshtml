@page
@model APIfilmes.Pages.filmes.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Produtos</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Produtos.ID" />
            <div class="form-group">
                <label asp-for="Produtos.Title" class="control-label"></label>
                <input asp-for="Produtos.Title" class="form-control" />
                <span asp-validation-for="Produtos.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Produtos.ReleaseDate" class="control-label"></label>
                <input asp-for="Produtos.ReleaseDate" class="form-control" />
                <span asp-validation-for="Produtos.ReleaseDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Produtos.Genre" class="control-label"></label>
                <input asp-for="Produtos.Genre" class="form-control" />
                <span asp-validation-for="Produtos.Genre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Produtos.Price" class="control-label"></label>
                <input asp-for="Produtos.Price" class="form-control" />
                <span asp-validation-for="Produtos.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
